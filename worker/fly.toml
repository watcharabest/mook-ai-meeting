app = "mook-ai-worker"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

# Worker ไม่มี HTTP — ใช้ processes แทน
[processes]
  worker = "celery -A app.celery_app worker --loglevel=info -Q transcription --concurrency=2"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
