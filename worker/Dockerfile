FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application
COPY . .

# Run Celery worker
CMD ["celery", "-A", "app.celery_app", "worker", "--loglevel=info", "-Q", "transcription", "--concurrency=2"]
